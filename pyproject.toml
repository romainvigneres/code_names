[project]
name = "code-names"
version = "0.1.0"
description = "Codenames board game with AI players"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "textual>=0.70.0",
    "ollama>=0.3.0",
]

[project.scripts]
code-names = "main:main"
